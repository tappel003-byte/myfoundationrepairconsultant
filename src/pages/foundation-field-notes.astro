---
import BaseLayout from '../layouts/BaseLayout.astro';

const logoSrc =
  'https://images.squarespace-cdn.com/content/v1/6983db0242b9b6302c661059/fa18c5f7-2e51-4ae1-81d8-0517c590d714/MFRC+Logo.jpg?format=500w';

const heardCards = [
  {
    id: 'piers',
    heard: 'They said I need piers.',
    might: 'Trying to move part of the load to deeper, more stable ground.',
    prove: 'What caused the movement, whether it\u2019s still happening, or that a deep load path is actually the fix.',
    ask: 'What problem are the piers meant to solve, and what tells you that?',
    urgent: false,
  },
  {
    id: 'bedrock',
    heard: 'They said we have to get to bedrock.',
    might: 'Bedrock really can be a strong bearing layer \u2014 reaching it is sometimes exactly right.',
    prove: 'How deep it is, whether it\u2019s actually competent, or how the system connects to it.',
    ask: 'What material did you find, at what depth, and how does the design use it?',
    urgent: false,
  },
  {
    id: 'void',
    heard: 'They said there\u2019s a void under the slab.',
    might: 'Soil can wash out or settle, leaving a real gap.',
    prove: 'That the foundation itself needs underpinning \u2014 a slab and a footing carry different loads.',
    ask: 'Is this slab part of the building\u2019s structural support, or just a floor that needs to sit right again?',
    urgent: false,
  },
  {
    id: 'lift',
    heard: 'They said they can lift it.',
    might: 'Some methods genuinely restore lost elevation, not just stop movement.',
    prove: 'Lift and stabilization are different goals \u2014 or how much lift, or what it does to plumbing and finishes.',
    ask: 'Is the goal to stop it, restore elevation, or both \u2014 and what happens to the rest of the house if it moves?',
    urgent: false,
  },
  {
    id: 'best',
    heard: 'They said this is the best system.',
    might: 'A specific method really can be the strongest fit for a specific site.',
    prove: '\u201cBest\u201d without a stated reason doesn\u2019t say why, for your house, specifically.',
    ask: 'Best for what condition \u2014 and compared to what?',
    urgent: false,
  },
  {
    id: 'now',
    heard: 'They said it needs to be fixed now.',
    might: 'Some things genuinely are urgent \u2014 active water, fast new movement, a real safety concern.',
    prove: 'Urgency in the pitch isn\u2019t urgency in the ground. Historic, stable movement usually isn\u2019t an emergency.',
    ask: 'What\u2019s actually changing right now \u2014 and what happens if I get a second opinion first?',
    urgent: true,
  },
];

const truthCards = [
  {
    id: 'same',
    claim: 'Helical piers and micropiles are the same thing.',
    answer: 'False',
    why: 'Rotated in with plates vs. drilled and grouted. Different mechanism entirely.',
  },
  {
    id: 'crack',
    claim: 'A crack always means the foundation is failing.',
    answer: 'False',
    why: 'Most cracks are ordinary material behavior, not structural distress.',
  },
  {
    id: 'foam',
    claim: 'Foam jacking is a type of pier.',
    answer: 'False',
    why: 'Foam fills voids and can lift a slab; it doesn\u2019t transfer load like a pier.',
  },
  {
    id: 'rock',
    claim: '\u201cBedrock\u201d guarantees a stable repair.',
    answer: 'False',
    why: 'It still has to be identified, verified, and properly connected.',
  },
  {
    id: 'quiet',
    claim: 'A quieter, cheaper method can sometimes be the right one.',
    answer: 'True',
    why: 'The right method matches the problem, not the loudest pitch.',
  },
];

const situations = [
  {
    id: 'access',
    text: 'Limited access, low headroom',
    accept: ['helical', 'micropiles'],
    note: 'More than one method can genuinely fit here \u2014 that\u2019s not a trick, that\u2019s real.',
  },
  {
    id: 'loose',
    text: 'Loose or voided soil that can be densified',
    accept: ['grout'],
  },
  {
    id: 'slabvoid',
    text: 'A void under a slab',
    accept: ['foam'],
  },
  {
    id: 'heavy',
    text: 'Heavy structure, settlement that\u2019s localized and mapped',
    accept: ['push'],
  },
  {
    id: 'name',
    text: 'A name on the bid you don\u2019t recognize',
    accept: ['other'],
  },
];

const methods = [
  { id: 'helical', text: 'Helical Piers' },
  { id: 'micropiles', text: 'Micropiles' },
  { id: 'grout', text: 'Compaction Grouting' },
  { id: 'foam', text: 'Foam Jacking' },
  { id: 'push', text: 'Push Piers' },
  { id: 'other', text: 'Other Deep Foundations' },
];
---

<BaseLayout
  title="What Did You Just Hear? \u2014 Foundation Field Notes"
  description="A small deck of things people hear about foundation repair \u2014 what they might mean, what they don\u2019t prove, and a better question to ask. Not a diagnosis. Not a sales page."
>
  <div class="notes">
    <p class="notes-eyebrow">Just for the Fun of It</p>
    <h1>What Did You Just Hear?</h1>
    <p class="notes-lede">
      Three short decks. No score. No right house. Tap a card, read what came out of someone\u2019s mouth, and see the better question underneath.
    </p>

    <section class="game" id="heard">
      <h2>What they said</h2>
      <p class="game-note">Tap once for the sentence. Tap again for what it does and does not prove.</p>
      <div class="deck deck-3" data-shuffle>
        {heardCards.map((card) => (
          <button
            class:list={['flip', card.urgent && 'is-urgent']}
            type="button"
            data-steps="3"
            aria-label={card.heard}
          >
            <span class="flip-inner">
              <span class="face face-down">
                <img src={logoSrc} alt="" />
              </span>
              <span class="face face-mid">
                <span class="kicker">Someone said</span>
                <span class="line">{card.heard}</span>
                <span class="hint">Tap again</span>
              </span>
              <span class="face face-up">
                <span class="bit"><strong>Might mean</strong>{card.might}</span>
                <span class="bit"><strong>Doesn\u2019t prove</strong>{card.prove}</span>
                <span class="bit ask"><strong>Ask instead</strong>{card.ask}</span>
              </span>
            </span>
          </button>
        ))}
      </div>
    </section>

    <section class="game" id="truth">
      <h2>True or false</h2>
      <p class="game-note">Tap once for the claim. Tap again for the call.</p>
      <div class="deck deck-3" data-shuffle>
        {truthCards.map((card) => (
          <button class="flip" type="button" data-steps="3" aria-label={card.claim}>
            <span class="flip-inner">
              <span class="face face-down">
                <img src={logoSrc} alt="" />
              </span>
              <span class="face face-mid">
                <span class="kicker">Claim</span>
                <span class="line">{card.claim}</span>
                <span class="hint">Tap again</span>
              </span>
              <span class="face face-up">
                <span class:list={['call', card.answer === 'True' && 'call-true']}>{card.answer}</span>
                <span class="why">{card.why}</span>
              </span>
            </span>
          </button>
        ))}
      </div>
    </section>

    <section class="game" id="match">
      <h2>Match it</h2>
      <p class="game-note">
        Flip a situation. Flip a method. Some situations have more than one honest answer.
      </p>
      <p class="match-status" id="matchStatus" hidden></p>
      <div class="match-board">
        <div>
          <h3>Situation</h3>
          <div class="deck deck-2" data-shuffle>
            {situations.map((sit) => (
              <button
                class="flip match-card"
                type="button"
                data-role="sit"
                data-id={sit.id}
                data-accept={sit.accept.join(',')}
                data-note={sit.note || ''}
                aria-label={sit.text}
              >
                <span class="flip-inner">
                  <span class="face face-down">
                    <img src={logoSrc} alt="" />
                  </span>
                  <span class="face face-up sit-face">
                    <span class="kicker">Situation</span>
                    <span class="line">{sit.text}</span>
                  </span>
                </span>
              </button>
            ))}
          </div>
        </div>
        <div>
          <h3>Method</h3>
          <div class="deck deck-2" data-shuffle>
            {methods.map((method) => (
              <button
                class="flip match-card"
                type="button"
                data-role="method"
                data-id={method.id}
                aria-label={method.text}
              >
                <span class="flip-inner">
                  <span class="face face-down">
                    <img src={logoSrc} alt="" />
                  </span>
                  <span class="face face-up method-face">
                    <span class="kicker">Method</span>
                    <span class="line">{method.text}</span>
                  </span>
                </span>
              </button>
            ))}
          </div>
        </div>
      </div>
    </section>

    <p class="notes-exit">
      <a href="/repair-methods">Back to Repair Methods \u2192</a>
    </p>
  </div>
</BaseLayout>

<style>
  .notes {
    max-width: 1080px;
    margin: 0 auto;
    padding: 48px 32px 80px;
  }
  .notes-eyebrow {
    font-family: 'DM Mono', monospace;
    font-size: 11px;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: #8a8a8a;
    margin: 0 0 12px;
  }
  .notes h1 {
    font-family: 'Playfair Display', serif;
    font-size: clamp(34px, 5vw, 52px);
    font-weight: 600;
    line-height: 1.15;
    margin: 0 0 16px;
    color: #1a1a1a;
  }
  .notes-lede {
    max-width: 640px;
    font-size: 18px;
    line-height: 1.65;
    color: #5a5a5a;
    margin: 0 0 48px;
  }
  .game {
    margin: 0 0 56px;
  }
  .game h2 {
    font-family: 'Playfair Display', serif;
    font-size: 28px;
    font-weight: 600;
    margin: 0 0 8px;
  }
  .game h3 {
    font-family: 'Playfair Display', serif;
    font-size: 18px;
    font-weight: 600;
    margin: 0 0 12px;
  }
  .game-note {
    color: #5a5a5a;
    margin: 0 0 20px;
    font-size: 16px;
  }
  .deck {
    display: grid;
    gap: 16px;
  }
  .deck-3 { grid-template-columns: repeat(3, 1fr); }
  .deck-2 { grid-template-columns: repeat(2, 1fr); }
  .match-board {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 28px;
  }
  .flip {
    appearance: none;
    border: 0;
    padding: 0;
    background: transparent;
    width: 100%;
    height: 240px;
    perspective: 1200px;
    cursor: pointer;
    text-align: left;
  }
  .flip-inner {
    position: relative;
    width: 100%;
    height: 100%;
    transform-style: preserve-3d;
    transition: transform 0.7s cubic-bezier(0.22, 1, 0.36, 1);
  }
  .flip[data-step='1'] .flip-inner,
  .flip[data-step='2'] .flip-inner { transform: rotateY(180deg); }
  .face {
    position: absolute;
    inset: 0;
    backface-visibility: hidden;
    -webkit-backface-visibility: hidden;
    border-radius: 10px;
    box-shadow: 0 10px 24px -18px rgba(0,0,0,0.45);
    overflow: hidden;
  }
  .face-down {
    background: #ececec;
    border: 1px solid #cfcfcf;
    display: grid;
    place-items: center;
    padding: 18px;
  }
  .face-down img {
    width: min(78%, 180px);
    height: auto;
    opacity: 0.7;
    display: block;
  }
  .face-mid,
  .face-up,
  .sit-face,
  .method-face {
    background: #fbf8f2;
    border: 1px solid #e2d9cc;
    padding: 18px 18px 16px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    transform: rotateY(180deg);
  }
  .flip[data-steps='3'] .face-up,
  .flip[data-steps='3'] .face-mid {
    transform: rotateY(180deg);
  }
  .flip[data-steps='3'] .face-up { opacity: 0; pointer-events: none; }
  .flip[data-steps='3'][data-step='2'] .face-up { opacity: 1; pointer-events: auto; }
  .flip[data-steps='3'][data-step='2'] .face-mid { opacity: 0; pointer-events: none; }
  .is-urgent .face-mid,
  .is-urgent .face-up {
    border-left: 4px solid var(--chile);
  }
  .is-urgent .face-down {
    border-left: 4px solid var(--chile);
  }
  .kicker,
  .hint {
    font-family: 'DM Mono', monospace;
    font-size: 10px;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: #8a8a8a;
  }
  .hint { margin-top: 14px; color: var(--teal); }
  .line {
    font-family: 'Playfair Display', serif;
    font-size: 20px;
    font-weight: 600;
    line-height: 1.3;
    color: #1a1a1a;
    margin-top: 8px;
  }
  .bit {
    display: block;
    font-size: 14px;
    line-height: 1.45;
    color: #2e2e2e;
    margin-bottom: 10px;
  }
  .bit strong {
    display: block;
    font-family: 'DM Mono', monospace;
    font-size: 10px;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    font-weight: 400;
    color: #8a8a8a;
    margin-bottom: 3px;
  }
  .bit.ask strong { color: var(--teal); }
  .call {
    font-family: 'Playfair Display', serif;
    font-size: 28px;
    font-weight: 700;
    color: #1a1a1a;
    margin-bottom: 10px;
  }
  .call-true { color: var(--teal); }
  .why {
    font-size: 15px;
    line-height: 1.5;
    color: #2e2e2e;
  }
  .match-card.is-picked .face-up {
    outline: 2px solid var(--teal);
    outline-offset: -2px;
  }
  .match-card.is-paired .face-up {
    background: #f3f1ea;
  }
  .match-card.is-wrong {
    animation: shake 0.35s ease;
  }
  .match-status {
    background: #f3f1ea;
    border-left: 3px solid var(--teal);
    padding: 12px 16px;
    margin: 0 0 16px;
    font-size: 16px;
  }
  .notes-exit {
    margin: 8px 0 0;
  }
  .notes-exit a {
    color: var(--teal);
    text-decoration: none;
    font-size: 17px;
  }
  .notes-exit a:hover { text-decoration: underline; text-underline-offset: 3px; }
  @keyframes shake {
    0%, 100% { transform: translateX(0); }
    25% { transform: translateX(-6px); }
    75% { transform: translateX(6px); }
  }
  @media (max-width: 900px) {
    .deck-3,
    .deck-2,
    .match-board { grid-template-columns: 1fr; }
    .notes { padding: 32px 20px 64px; }
    .flip { height: 220px; }
  }
</style>

<script>
  const shuffle = (node) => {
    const cards = Array.from(node.children);
    for (let i = cards.length - 1; i > 0; i -= 1) {
      const j = Math.floor(Math.random() * (i + 1));
      node.appendChild(cards[j]);
      cards.splice(j, 1, cards[i]);
    }
  };
  document.querySelectorAll('[data-shuffle]').forEach(shuffle);

  document.querySelectorAll('.flip:not(.match-card)').forEach((card) => {
    const steps = Number(card.getAttribute('data-steps') || 2);
    card.addEventListener('click', () => {
      const now = Number(card.getAttribute('data-step') || 0);
      const next = (now + 1) % steps;
      card.setAttribute('data-step', String(next));
    });
  });

  let pickedSit = null;
  let pickedMethod = null;
  const status = document.getElementById('matchStatus');

  const clearPicks = () => {
    document.querySelectorAll('.match-card.is-picked').forEach((el) => el.classList.remove('is-picked'));
    pickedSit = null;
    pickedMethod = null;
  };

  const tryMatch = () => {
    if (!pickedSit || !pickedMethod) return;
    const accept = (pickedSit.getAttribute('data-accept') || '').split(',');
    const methodId = pickedMethod.getAttribute('data-id');
    const ok = accept.includes(methodId);
    if (ok) {
      pickedSit.classList.add('is-paired');
      pickedMethod.classList.add('is-paired');
      const note = pickedSit.getAttribute('data-note');
      if (note && status) {
        status.hidden = false;
        status.textContent = note;
      }
      clearPicks();
    } else {
      pickedSit.classList.add('is-wrong');
      pickedMethod.classList.add('is-wrong');
      window.setTimeout(() => {
        pickedSit?.classList.remove('is-wrong');
        pickedMethod?.classList.remove('is-wrong');
        clearPicks();
      }, 420);
    }
  };

  document.querySelectorAll('.match-card').forEach((card) => {
    card.addEventListener('click', () => {
      if (card.classList.contains('is-paired')) return;
      const open = card.getAttribute('data-step') === '1';
      if (!open) {
        card.setAttribute('data-step', '1');
        return;
      }
      const role = card.getAttribute('data-role');
      if (role === 'sit') {
        document.querySelectorAll('.match-card[data-role="sit"].is-picked').forEach((el) => el.classList.remove('is-picked'));
        pickedSit = card;
      } else {
        document.querySelectorAll('.match-card[data-role="method"].is-picked').forEach((el) => el.classList.remove('is-picked'));
        pickedMethod = card;
      }
      card.classList.add('is-picked');
      tryMatch();
    });
  });
</script>
